# 需求文档 (PRD)：采购合同台账智能管理 Agent 系统

**文档版本**：V 1.0
**所属部门**：运维技术部
**应用单位**：云南云上云信息化有限公司
**产品形态**：Windows 桌面端 (.exe) + 钉钉移动端

## 1. 项目概述

### 1.1 业务背景
目前，运维技术部在管理《采购合同履行情况统计表》时，面临数据结构复杂（包含第3、4行双层表头及多组平行的付款金额/时间字段）、高频碎片化信息更新易错漏、人工核对费时等痛点。现亟需开发一款智能辅助工具，通过自然语言交互实现全生命周期的自动化操作。

### 1.2 建设目标
构建一个“LLM 中枢 + 定制化脚本工具箱”的 Agent 系统。系统需具备理解日常对话、精准解析复杂 Excel 物理结构的能力。实现前端轻量交互，后端 100% 确定性写入，并最终以 `.exe` 形式打包，满足 Windows 办公环境下的开箱即用及钉钉多端协同。

## 2. 系统总体架构

系统采用业务解耦架构，包含以下三个核心层：

* **交互展现层 (UI/UX)**
  * **PC 桌面端**：基于前端框架（如 Vue/React）开发，使用 `pywebview` 套壳，结合 `PyInstaller` 打包为免安装的本地原生 `.exe` 程序。
  * **移动协同端**：对接钉钉机器人，利用钉钉互动卡片（ActionCard）实现移动端的信息录入、查询与关键操作的二次确认。
* **逻辑调度层 (LLM Router)**
  * 作为中枢大脑，接收自然语言输入，执行意图识别 (NLU)、实体抽取，并将其转换为标准 JSON 格式的工具调用指令 (Function Calling)。
* **数据执行层 (Toolbox)**
  * **核心底座**：采用 Python (`xlwings` 库) 或底层 VBA 脚本进行文件 I/O。
  * **原则**：直接调用系统底层组件操作原始表格，严禁破坏既有的合并单元格、多级表头以及“合同未付款合计”等内置计算公式。

## 3. 全生命周期业务功能说明

### 3.1 合同初始建档 (Initiation)
* **业务描述**：新合同签订后的基础信息初始化。
* **输入方式**：自然语言口述或前端表单（如：“新增核心交换机扩容合同，对方是华为，金额50万，税率13%”）。
* **处理逻辑**：
  1. 寻址读取当前最大“序号”，新起一行累加。
  2. 提取并填入基础字段（对应销售合同、合同名称、对方单位名称等）。
  3. 脚本自动计算“不含税金额”：`合同金额 / (1 + 税率)`。
  4. 初始化“合同状态”为`执行中`，“已开票情况”为`未开票`。

### 3.2 执行节点动态更新 (Execution & Milestones)
* **业务描述**：对“初验”、“终验”等碎片的执行节点进行穿透式精准填报。
* **输入方式**：自然语言触发（如：“交换机项目今天初验通过”）。
* **处理逻辑**：
  1. **模糊检索**：通过合同名称或对方单位名称，定位唯一目标行（如存在多条相似记录，触发反问交互）。
  2. **双层表头穿透**：定位目标行后，脚本穿透“合同执行节点”主表头，精确定位至“初验日期”等子表头对应单元格进行时间戳写入。

### 3.3 动态资金支付追加 (Payment & Invoicing) —— 【核心难点】
* **业务描述**：应对表格尾部多组无限延伸的“付款金额”与“付款时间”列。
* **处理逻辑**：
  1. 提取当前付款金额与日期。
  2. **游标空位寻址**：脚本锁定目标行后，向右遍历匹配该行的所有“付款金额”单元格。当遇到第一个状态为“空”或“NaN”的单元格时，驻留并写入本次金额，相邻列写入付款日期。
  3. **防呆校验机制**：写入前执行预校验，若 `本次付款金额 + 当前付款合计 > 合同总金额`，则阻断写入并在前端弹窗/钉钉报错。

### 3.4 质保与结项主动预警 (Closure)
* **业务描述**：系统从被动记录转换为主动业务推动。
* **处理逻辑**：
  1. **状态流转**：当侦测到某行“退履约保证金、质保金”字段已更新，且“合同未付款合计”为 0，自动将“合同状态”改为`已结项`。
  2. **定时巡检 (Cron Job)**：后台服务每日静默读取“截止日期（以质保/维保结束时间为准）”列。对于处于`执行中`且将在 7 日内到期的合同，向经办人或钉钉群推送预警卡片。

## 4. 人机协同与安全规范 (Human-in-the-loop)

针对涉及真实金额及台账修改的敏感操作，强制实行以下闭环安全规范：
1. **指令解析隔离**：LLM 仅负责生成 JSON 参数，绝对不直接具备文件覆写权限。
2. **操作二次确认**：所有“增、改”动作生成后，系统必须向 PC 界面或钉钉推送确认卡片（呈现修改前/修改后对比）。
3. **闭环落盘**：仅当用户点击【确认执行】后，Python/VBA 底层脚本才执行物理层面的 Excel 文件保存。

## 5. 数据字典与接口映射表 (部分关键核心映射)

为指导底层脚本开发，设定以下字段物理映射：

| 业务字段类别 | 对应物理表头 (行3/行4) | 读写权限 | Agent/脚本处理规则 |
| :--- | :--- | :--- | :--- |
| **主键与索引** | 序号, 合同名称, 对方单位 | 读写 | 作为检索 Key 进行行定位 |
| **基础财务** | 合同金额, 税率, 不含税金额 | 读写 | 不含税金额需后台运算后填入 |
| **生命周期** | 合同执行节点 -> 初验日期 / 终验日期 | 精确写 | **需构建多级字典索引寻址** |
| **支付与发票** | 付款金额, 付款时间 (多组) | 动态寻址 | 执行“找空位追加”算法，禁忌覆盖 |
| **自动计算项** | 合同未付款合计, 付款合计 | **严格只读** | 依赖 Excel 自身函数，脚本禁写 |

---
*注：本项目初期开发重心应优先放在跑通【3.3 动态资金支付追加】的 xlwings 本地寻址写入脚本。此功能通过验证后，外层 API 封装、套壳打包及钉钉接入即可顺畅完成。*
