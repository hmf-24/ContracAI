# ContracAI Dependencies

# --- Web Framework & Desktop Shell ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pywebview>=5.0

# --- Excel Engine (requires MS Excel installed) ---
xlwings>=0.32.0

# --- LLM Communication (OpenAI-compatible HTTP) ---
httpx>=0.27.0

# --- Document Parsing ---
pdfplumber>=0.11.0       # Text-based PDF extraction
pdf2image>=1.17.0        # Scanned PDF -> images (requires poppler)
python-docx>=1.1.0       # Word .docx parsing
Pillow>=10.0.0           # Image processing

# --- Packaging ---
pyinstaller>=6.0         # Build standalone .exe

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23.0
